<Window x:Class="desktopapp.Views.AddTaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:desktopapp.Views"
        mc:Ignorable="d"
        Title="Dodaj / Edytuj Zadanie" SizeToContent="Height" Width="450" WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType" UseLayoutRounding="True">
    
    <Border Background="White" Padding="20">
        <StackPanel>
            <TextBlock Text="Szczegóły zadania" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>

            <TextBlock Text="Tytuł:" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" />

            <TextBlock Text="Opis:" Style="{StaticResource FormLabel}"/>
            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" Margin="0,0,0,15" />

            <TextBlock Text="Termin wykonania:" Style="{StaticResource FormLabel}"/>
            <DatePicker SelectedDate="{Binding DueDate, UpdateSourceTrigger=PropertyChanged}" Height="35" BorderBrush="#dee2e6" Padding="5,0" Margin="0,0,0,15"/>

            <TextBlock Text="Przypisz do:" Style="{StaticResource FormLabel}"/>
            <ComboBox ItemsSource="{Binding AvailableUsernames}" SelectedItem="{Binding AssignedUser, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" />

            <TextBlock Text="Projekt:" Style="{StaticResource FormLabel}"/>
            <ComboBox ItemsSource="{Binding AvailableProjects}" 
                      DisplayMemberPath="Name" 
                      SelectedItem="{Binding SelectedProject}" 
                      Margin="0,0,0,15"/>
            
            <TextBlock Text="Status:" Style="{StaticResource FormLabel}"/>
            <ComboBox SelectedItem="{Binding Status}" Margin="0,0,0,25">
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Do zrobienia</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">W trakcie</System:String>
                <System:String xmlns:System="clr-namespace:System;assembly=mscorlib">Zakończone</System:String>
            </ComboBox>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="Anuluj" Style="{StaticResource SecondaryButton}" Command="{Binding CloseCommand}"/>
                <Button Grid.Column="2" Content="Zapisz" Style="{StaticResource PrimaryButton}" Command="{Binding SaveCommand}"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>