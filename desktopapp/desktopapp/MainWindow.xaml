<Window x:Class="desktopapp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:desktopapp.ViewModels"
        mc:Ignorable="d"
        Title="Taskomat - Panel" Height="600" Width="1000" WindowStartupLocation="CenterScreen"
        TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType" UseLayoutRounding="True">

    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <vm:StringToBoolConverter x:Key="ViewToCheckedConverter"/>
        
        <Style TargetType="TextBlock" x:Key="DataGridCellPadding">
            <Setter Property="Margin" Value="10,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" Padding="15,10">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="5" Direction="270"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Taskomat" FontWeight="Bold" FontSize="20" VerticalAlignment="Center" Margin="10,0,30,0"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton Content="Zadania" Style="{StaticResource MenuLinkButton}" IsChecked="{Binding SelectedView, Converter={StaticResource ViewToCheckedConverter}, ConverterParameter='Zadania'}" Command="{Binding SwitchViewCommand}" CommandParameter="Zadania"/>
                    <RadioButton Content="Projekty" Style="{StaticResource MenuLinkButton}" IsChecked="{Binding SelectedView, Converter={StaticResource ViewToCheckedConverter}, ConverterParameter='Projekty'}" Command="{Binding SwitchViewCommand}" CommandParameter="Projekty"/>
                    <RadioButton Content="🔔 Centrum Powiadomień" Style="{StaticResource MenuLinkButton}" IsChecked="{Binding SelectedView, Converter={StaticResource ViewToCheckedConverter}, ConverterParameter='Notifications'}" Command="{Binding SwitchViewCommand}" CommandParameter="Notifications"/>
                    <RadioButton Content="Ustawienia" Style="{StaticResource MenuLinkButton}" IsChecked="{Binding SelectedView, Converter={StaticResource ViewToCheckedConverter}, ConverterParameter='Ustawienia'}" Command="{Binding SwitchViewCommand}" CommandParameter="Ustawienia"/>
                </StackPanel>

                <Button Grid.Column="2" Content="Logout" Style="{StaticResource DangerButton}" Command="{Binding LogoutCommand}" VerticalAlignment="Center" Margin="0,0,10,0"/>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="20">
            
            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedView}" Value="Zadania">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Margin="0,0,0,15">
                    <TextBlock Text="Zarządzanie Zadaniami" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                        <Button Content="+ Dodaj zadanie" Style="{StaticResource PrimaryButton}" Command="{Binding OpenAddTaskWindowCommand}" Margin="0,0,15,0"/>
                        <TextBlock Text="Filtruj wg:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextDarkGray}"/>
                        <TextBox Width="150" Text="{Binding SearchUserText, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,10,0" VerticalContentAlignment="Center"/>
                        <Border BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="4" Margin="0,0,10,0" Background="White">
                            <ComboBox Width="150" ItemsSource="{Binding Projects}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedProjectFilter, UpdateSourceTrigger=PropertyChanged}" Padding="5" VerticalContentAlignment="Center" BorderThickness="0"/>
                        </Border>
                        <DatePicker Width="120" SelectedDate="{Binding SelectedFilterDate, UpdateSourceTrigger=PropertyChanged}" Height="35" BorderBrush="{StaticResource BorderGray}" Padding="5,0" Margin="0,0,5,0"/>
                        <Button Content="X" Command="{Binding ClearFilterDateCommand}" Background="Transparent" Foreground="{StaticResource DangerRed}" BorderThickness="0" Margin="0,0,10,0" ToolTip="Wyczyść datę"/>
                        <CheckBox Content="Tylko moje zadania" IsChecked="{Binding ShowOnlyMyTasks, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" Foreground="{StaticResource PrimaryBlue}"/>
                    </StackPanel>
                </StackPanel>

                <DataGrid Grid.Row="1" ItemsSource="{Binding Tasks}" SelectedItem="{Binding SelectedTask}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowBackground="White" AlternatingRowBackground="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" GridLinesVisibility="None" RowHeight="40">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="White"/>
                            <Setter Property="Foreground" Value="{StaticResource TextDarkGray}"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10,10"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Resources>
                        <Style TargetType="DataGridCell" x:Key="StatusCellStyle">
                            <Setter Property="Padding" Value="10,0"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="Black"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50" ElementStyle="{StaticResource DataGridCellPadding}" />
                        <DataGridTextColumn Header="Tytuł" Binding="{Binding Title}" Width="*" ElementStyle="{StaticResource DataGridCellPadding}"/>
                        <DataGridTextColumn Header="Przypisany" Binding="{Binding AssignedUser}" Width="150" ElementStyle="{StaticResource DataGridCellPadding}"/>
                        <DataGridTextColumn Header="Termin" Binding="{Binding DueDate, StringFormat=\{0:dd.MM.yyyy\}}" Width="100" ElementStyle="{StaticResource DataGridCellPadding}"/>
                        <DataGridTemplateColumn Header="Status" Width="130" CellStyle="{StaticResource StatusCellStyle}">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="12" Margin="5,0">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding DisplayStatus}" Value="Do zrobienia">
                                                        <Setter Property="Background" Value="#f8d7da"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding DisplayStatus}" Value="W trakcie">
                                                        <Setter Property="Background" Value="#fff3cd"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding DisplayStatus}" Value="Zakończone">
                                                        <Setter Property="Background" Value="#d1e7dd"/>
                                                    </DataTrigger>
                                                    </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding DisplayStatus}" FontWeight="SemiBold" Padding="10,4" HorizontalAlignment="Center">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DisplayStatus}" Value="Do zrobienia">
                                                            <Setter Property="Foreground" Value="#842029"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DisplayStatus}" Value="W trakcie">
                                                            <Setter Property="Foreground" Value="#664d03"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding DisplayStatus}" Value="Zakończone">
                                                            <Setter Property="Foreground" Value="#0f5132"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <DataGridTemplateColumn Header="Akcje" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" Margin="10,0">
                                        <Button Content="Update" Foreground="{StaticResource PrimaryBlue}" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.EditTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Cursor="Hand" Margin="0,0,10,0"/>
                                        <Button Content="Delete" Foreground="{StaticResource DangerRed}" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.DeleteTaskCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Cursor="Hand"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedView}" Value="Projekty">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,20,0">
                    <TextBlock Text="Lista projektów" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <DataGrid ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column" Background="White" RowBackground="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" GridLinesVisibility="None" RowHeight="40">
                        <DataGrid.ColumnHeaderStyle>
                            <Style TargetType="DataGridColumnHeader">
                                <Setter Property="Background" Value="White"/>
                                <Setter Property="Foreground" Value="{StaticResource TextDarkGray}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                                <Setter Property="Padding" Value="10"/>
                                <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            </Style>
                        </DataGrid.ColumnHeaderStyle>
                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                            </Style>
                        </DataGrid.RowStyle>
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="50" ElementStyle="{StaticResource DataGridCellPadding}"/>
                            <DataGridTextColumn Header="Nazwa" Binding="{Binding Name}" Width="150" ElementStyle="{StaticResource DataGridCellPadding}"/>
                            <DataGridTextColumn Header="Opis" Binding="{Binding Description}" Width="*" ElementStyle="{StaticResource DataGridCellPadding}"/>
                            <DataGridTextColumn Header="Właściciel" Binding="{Binding Owner.Username}" Width="150" ElementStyle="{StaticResource DataGridCellPadding}"/>
                            <DataGridTemplateColumn Header="Akcje">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" Margin="10,0">
                                            <Button Content="Update" Foreground="{StaticResource PrimaryBlue}" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.PrepareEditProjectCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Cursor="Hand" Margin="0,0,10,0"/>
                                            <Button Content="Delete" Foreground="{StaticResource DangerRed}" Background="Transparent" BorderThickness="0" Command="{Binding DataContext.DeleteProjectCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" Cursor="Hand"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>

                <Border Grid.Column="1" Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="4" Padding="20" VerticalAlignment="Top">
                    <StackPanel>
                        <TextBlock Text="{Binding ProjectFormTitle}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                        <TextBlock Text="Nazwa projektu" Foreground="{StaticResource TextDarkGray}" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewProjectName, UpdateSourceTrigger=PropertyChanged}" Padding="5" Margin="0,0,0,15"/>
                        <TextBlock Text="Opis" Foreground="{StaticResource TextDarkGray}" Margin="0,0,0,5"/>
                        <TextBox Text="{Binding NewProjectDescription, UpdateSourceTrigger=PropertyChanged}" Height="80" TextWrapping="Wrap" Padding="5" Margin="0,0,0,20"/>
                        <Button Content="{Binding ProjectButtonText}" Style="{StaticResource PrimaryButton}" Command="{Binding AddProjectCommand}"/>
                        <Button Content="Anuluj" Command="{Binding CancelEditProjectCommand}" Background="Transparent" Foreground="{StaticResource TextDarkGray}" BorderThickness="0" Margin="0,10,0,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Visibility" Value="Visible"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding ProjectButtonText}" Value="Dodaj projekt">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid>
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedView}" Value="Notifications">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,20">
                    <TextBlock Text="Oczekujące Zaproszenia" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <ItemsControl ItemsSource="{Binding PendingInvitations}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center">
                                            <WrapPanel>
                                                <TextBlock Text="Zostałeś zaproszony do projektu:" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding ProjectName}" FontWeight="Bold" Margin="5,0,5,0" VerticalAlignment="Center"/>
                                                <TextBlock Text="przez" VerticalAlignment="Center"/>
                                                <TextBlock Text="{Binding InvitedBy}" FontWeight="Bold" Margin="5,0,0,0" VerticalAlignment="Center"/>
                                            </WrapPanel>
                                        </StackPanel>
                                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                                            <Button Content="Akceptuj" Style="{StaticResource PrimaryButton}" Command="{Binding DataContext.AcceptInvitationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding Id}" Margin="0,0,10,0"/>
                                            <Button Content="Odrzuć" Style="{StaticResource DangerButton}" Command="{Binding DataContext.RejectInvitationCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}" CommandParameter="{Binding Id}"/>
                                        </StackPanel>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>

                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/> </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="Historia Powiadomień" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="0,0,15,0">
                        <ItemsControl ItemsSource="{Binding Notifications}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="4" Padding="15" Margin="0,0,0,10">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <StackPanel VerticalAlignment="Center">
                                                <StackPanel Orientation="Horizontal" Margin="0,0,0,2">
                                                    <TextBlock Text="{Binding Type, TargetNullValue='system', FallbackValue='system'}" FontWeight="Bold" Foreground="Gray" FontSize="11"/>
                                                    <TextBlock Text=" • " Foreground="Gray" FontSize="11" Margin="3,0"/>
                                                    <TextBlock Text="{Binding TaskTitle}" Foreground="{StaticResource PrimaryBlue}" FontWeight="SemiBold" FontSize="11"/>
                                                </StackPanel>
                                                
                                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" FontSize="14"/>
                                            </StackPanel>
                                            <TextBlock Grid.Column="1" Text="{Binding CreatedAt, StringFormat=\{0:dd.MM.yyyy HH:mm\}}" Foreground="Gray" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Grid>

            <Border Background="White" BorderBrush="{StaticResource BorderGray}" BorderThickness="1" CornerRadius="4" Padding="30" HorizontalAlignment="Left" VerticalAlignment="Top">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedView}" Value="Ustawienia">
                                <Setter Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel Width="300">
                    <TextBlock Text="Ustawienia Profilu" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,20"/>
                    <TextBlock Text="Nazwa użytkownika" Foreground="{StaticResource TextDarkGray}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CurrentUserName}" IsReadOnly="True" Padding="5" Margin="0,0,0,15" Background="{StaticResource BgLightGray}"/>
                    
                    <TextBlock Text="Adres email" Foreground="{StaticResource TextDarkGray}" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CurrentUserEmail}" IsReadOnly="True" Padding="5" Margin="0,0,0,15" Background="{StaticResource BgLightGray}"/>
                    
                    <Border BorderBrush="{StaticResource BorderGray}" BorderThickness="0,1,0,0" Margin="0,10,0,20"/>

                    <TextBlock Text="Nowe hasło" Foreground="{StaticResource TextDarkGray}" Margin="0,0,0,5"/>
                    
                    <PasswordBox PasswordChanged="NewPasswordBox_PasswordChanged" Padding="5" Margin="0,0,0,20"/>

                    <Button Content="Zapisz zmiany" Style="{StaticResource PrimaryButton}" Command="{Binding SaveProfileCommand}"/>
                </StackPanel>
            </Border>
            
        </Grid>

        <!-- Powiadomienia -->
        <Border Grid.Row="1" Background="#333" CornerRadius="4" Padding="15,10" Margin="20" HorizontalAlignment="Right" VerticalAlignment="Bottom" Panel.ZIndex="1000">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Notifier.IsVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <TextBlock Text="{Binding Notifier.Message}" Foreground="White" FontWeight="SemiBold"/>
        </Border>

    </Grid>
</Window>