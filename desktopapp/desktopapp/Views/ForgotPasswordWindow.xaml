<Window x:Class="desktopapp.Views.ForgotPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resetowanie hasła" Height="300" Width="400" WindowStartupLocation="CenterScreen" 
        Background="#F5F5F5" ResizeMode="NoResize">
    <Grid>
        <Border Background="White" Margin="20" Padding="20" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="2" BlurRadius="5"/>
            </Border.Effect>
            <StackPanel VerticalAlignment="Center">
                <TextBlock Text="Zresetuj hasło" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                <TextBlock Text="Podaj email powiązany z Twoim kontem." Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Adres email:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" 
                         Height="35" 
                         Padding="5,0,5,0" 
                         VerticalContentAlignment="Center" 
                         Margin="0,0,0,20" 
                         FontSize="14"/>

                <Button Content="Wyślij link resetujący" Command="{Binding ResetPasswordCommand}" 
                        Background="#0078D7" Foreground="White" Height="35" FontWeight="Bold" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>