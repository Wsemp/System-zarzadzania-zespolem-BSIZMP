<Window x:Class="desktopapp.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:desktopapp.Views"
        xmlns:vm="clr-namespace:desktopapp.ViewModels" 
        mc:Ignorable="d"
        Title="LoginView" Height="450" Width="800">

    <Window.DataContext>
        <vm:LoginViewModel />
    </Window.DataContext>

    <Grid Width="300" Height="250">
        <StackPanel Margin="20" VerticalAlignment="Center">
            <TextBlock Text="Logowanie do Systemu" FontSize="18" FontWeight="Bold" Margin="0,0,0,20" HorizontalAlignment="Center"/>

            <TextBlock Text="Nazwa użytkownika:" />
            <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10" TextChanged="TextBox_TextChanged" />

            <TextBlock Text="Hasło:" />
            <PasswordBox x:Name="MyPasswordBox" PasswordChanged="MyPasswordBox_PasswordChanged" Margin="0,0,0,10" />

            <Button Content="Zaloguj" Command="{Binding LoginCommand}" Height="30" />
            <Button Content="Nie masz konta? Zarejestruj się" Command="{Binding OpenRegisterWindowCommand}" Background="Transparent" BorderThickness="0" Foreground="#0078D7" Margin="0,15,0,0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>