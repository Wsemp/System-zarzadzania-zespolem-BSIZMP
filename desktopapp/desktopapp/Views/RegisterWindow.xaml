<Window x:Class="desktopapp.Views.RegisterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:desktopapp.Views"
        mc:Ignorable="d"
        Title="Rejestracja - Taskomat" Height="550" Width="800" WindowStartupLocation="CenterScreen" Background="{StaticResource BgLightGray}"
        UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display" TextOptions.TextRenderingMode="ClearType">

    <Grid>
        <!-- Wycentrowana biała karta formularza z cieniem -->
        <Border Background="White" Width="350" Padding="30" CornerRadius="8" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="Black" Opacity="0.1" ShadowDepth="4" BlurRadius="10" Direction="270"/>
            </Border.Effect>
            <StackPanel>
                <TextBlock Text="Utwórz konto" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextDarkGray}" HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock Text="Zarejestruj się w systemie Taskomat" FontSize="14" Foreground="Gray" HorizontalAlignment="Center" Margin="0,0,0,25"/>

                <TextBlock Text="Nazwa użytkownika" Style="{StaticResource FormLabel}"/>
                <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" />

                <TextBlock Text="Adres e-mail" Style="{StaticResource FormLabel}"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,15" />

                <TextBlock Text="Hasło" Style="{StaticResource FormLabel}"/>
                <PasswordBox PasswordChanged="PasswordBox_PasswordChanged" Margin="0,0,0,15" />

                <TextBlock Text="Powtórz hasło" Style="{StaticResource FormLabel}"/>
                <PasswordBox PasswordChanged="ConfirmPasswordBox_PasswordChanged" Margin="0,0,0,25" />

                <Button Content="Zarejestruj się" Command="{Binding RegisterCommand}" Style="{StaticResource PrimaryButton}" Height="40" FontSize="16" FontWeight="SemiBold"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>